# Core scraping dependency
crawlforai

# HTML to Markdown conversion
html2text>=2020.1.16

# Configuration file support (YAML)
pyyaml>=6.0

# Optional: Better HTTP handling
requests>=2.28.0

# Optional: Progress bars for batch operations
tqdm>=4.65.0
